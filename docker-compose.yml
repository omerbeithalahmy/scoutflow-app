services:
  db:
    build: ./db
    container_name: scoutflow-db
    ports:
      - "5432:5432"
    env_file:
      - ./.env
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "${POSTGRES_USER}", "-d", "${POSTGRES_DB}"]
      interval: 2s
      retries: 10

  ingest:
    build: ./ingest
    container_name: scoutflow-ingest
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - ./.env
    restart: "no"
